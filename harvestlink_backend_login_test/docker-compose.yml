services:
  api:
    build: .
    ports:
      - "8001:8000"
    environment:
      DATABASE_URL: sqlite+aiosqlite:///./harvestlink.db
      SECRET_KEY: change-this-secret-key
      BACKEND_CORS_ORIGINS: http://localhost:5173,http://localhost:3000,http://127.0.0.1:5173
    volumes:
      - .:/app
